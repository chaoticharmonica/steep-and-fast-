# notes
open a terminal
go to this project: cd ~/Desktop/creative-coding-2/steepnfast





server stuff:
node needs to be installed

git needs to be installed

once both installed open git bash

running p5:
apen terminal type git bash
navigate to this project
run the server: http- server -c-1

point browser to localhost 8080
to stop server use ctrl c 
to install server: npm install -g http-server or sudo npm install -g http-server
while working on project:

open terminal, go to project
if new files added: git add
to make changes to github: git commit -am'commit message goes here'
git push